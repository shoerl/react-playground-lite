import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>
      <h1>Example App</h1>
      <p>The playground is available at <code>/__rplite</code></p>
    </div>
  </React.StrictMode>
);
